export enum ReadEditScreenType {
  READ,
  EDIT,
}

export const Screens = {
  HOME: 'HOME',
  READ_EDIT: 'READ_EDIT',
};

export enum InputActionType {
  Summarize = 'SUMMARIZE',
  Rewrite = 'REWRITE',
  Default = 'DEFAULT',
}
